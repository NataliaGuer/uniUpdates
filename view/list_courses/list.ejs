📃 I corsi disponibili sono:<% courses.forEach(course => { %>
&#8226; <b><%= course.name %></b> (id: <%= course.id %>) <% }) %>

Per ottenere maggiori informazioni utilizza il comando <%= moreInfo %>.